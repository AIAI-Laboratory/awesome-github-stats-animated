{
  "id": "particle-collider",
  "name": "Git Particle Collider",
  "description": "Repository particles follow intersecting orbits, collide and illuminate a contribution detector.",
  "version": "2.0.0",
  "author": "AIAI Laboratory",
  "license": "MIT",
  "entry": "./index.js",
  "width": 980,
  "height": 470,
  "tags": [
    "particle-collider",
    "animated",
    "auto-theme"
  ],
  "options": {
    "theme": {
      "type": "select",
      "default": "auto",
      "description": "Auto follows the viewer color scheme inside the SVG.",
      "choices": [
        "auto",
        "dark",
        "light"
      ]
    },
    "accent": {
      "type": "color",
      "default": "#ff5a4f",
      "description": "Primary highlight color. The default preserves the original theme palette."
    },
    "particleA": {
      "type": "color",
      "default": "#43e2ff",
      "description": "Custom palette color 2 for particles and beams."
    },
    "particleB": {
      "type": "color",
      "default": "#ffc857",
      "description": "Custom palette color 3 for particles and beams."
    },
    "particleC": {
      "type": "color",
      "default": "#ba77ff",
      "description": "Custom palette color 4 for particles and beams."
    },
    "particleD": {
      "type": "color",
      "default": "#63e6a3",
      "description": "Custom palette color 5 for particles and beams."
    }
  }
}
